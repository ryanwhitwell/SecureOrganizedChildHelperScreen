@using Sochs.Library.Events;
@using Sochs.Library.Interfaces;
@using Sochs.Library;
@using System.Runtime;
@inject ILunchService _lunchService;
@implements IDisposable;

<div class="col-lg-12 text-center" style="background-color: blue;">
  <h3 class="display" style="font-size: 6rem;">Lunch Menu</h3>
</div>

@code {
  protected override void OnInitialized()
  {
    _lunchService.OnLunchUpdated += Handle;

    base.OnInitialized();
  }

  public void Handle(object? sender, LunchUpdatedEventArgs args)
  {
    if (args != null)
    {
      // TODO: Deal with new lunch menus being available
      
      StateHasChanged();
    }
  }

  public void Dispose()
  {
    _lunchService.OnLunchUpdated -= Handle;
  }
}
